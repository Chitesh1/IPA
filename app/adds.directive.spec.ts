import { AddsDirective } from './adds.directive';

describe('AddsDirective', () => {
  it('should create an instance', () => {
    const directive = new AddsDirective();
    expect(directive).toBeTruthy();
  });
});
